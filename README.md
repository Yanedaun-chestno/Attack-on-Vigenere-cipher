# Project Title

This project takes text (`userText`) and key (`userKey`), and turns it into encrypted message using different ROTs for every symbol in `userText` (the so-called **Vigenère cipher**). The future steps are going to be attempts of cracking this cipher using **Kasiski examination**.